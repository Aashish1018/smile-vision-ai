# Teeth Simulator API

## Setup

1. Add `HF_API_TOKEN` in your `.env`.
2. Start the API:

```bash
npm run dev:api
```

## Endpoints

- `GET /health`
- `POST /api/teeth/simulate` (multipart/form-data with `image`)
